'use client';

import { FC } from 'react';
import { CV } from '~/modules/cv/cv.type';

type Props = {
  data: CV;
};

export const ExpandRow: FC<Props> = () => {
  return <div>ini adalah detail data</div>;
};
