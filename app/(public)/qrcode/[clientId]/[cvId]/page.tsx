import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Legacy QR Reference',
};

export default function Page() {
  return (
    <main className="container mx-auto p-6">
      <p className="text-muted-foreground text-sm">Legacy CV QR pages are retired from the active marketplace product.</p>
    </main>
  );
}
